@model ChamadoListViewModel

@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table ">
    <thead>
        <tr>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th>
                "DataInicio"
            </th>
            <th></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Chamados)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DataInicio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataFechamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Conclusao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Modalidade.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Gravidade.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Urgencia.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tendencia.Descricao)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ChamadoId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ChamadoId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ChamadoId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
